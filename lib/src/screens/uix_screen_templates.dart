import 'package:flutter/widgets.dart';

class UixScreenTemplate extends StatelessWidget {
  const UixScreenTemplate({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
